{{!-- character-header.hbs — always-visible header --}}
<div class="sheet-header">
  <div class="sidebar-accent"></div>
  <div class="header-main">
    <div class="field-group name-group">
      <label class="field-label">Paragon Name</label>
      <input type="text" name="name" value="{{actor.name}}" class="paragon-name-input" />
    </div>
    <div class="field-group alias-group">
      <label class="field-label">Character Alias</label>
      <input type="text" name="system.secretId" value="{{system.secretId}}" />
    </div>
    <div class="field-group pronouns-group">
      <label class="field-label">Pronouns</label>
      <input type="text" name="system.pronouns" value="{{system.pronouns}}" />
    </div>
    <div class="field-group player-group">
      <label class="field-label">Player Name</label>
      <input type="text" name="system.playerName" value="{{system.playerName}}" />
    </div>
  </div>
  <div class="header-sub">
    <div class="field-group level-group">
      <label class="field-label">Level</label>
      <input type="number" name="system.level" value="{{system.level}}" min="1" max="20" />
    </div>
    <div class="field-group archetype-group">
      <label class="field-label">Archetype</label>
      <select name="system.archetype">
        <option value="">— Choose —</option>
        {{#each archetypeChoices}}
        <option value="{{value}}" {{#if selected}}selected{{/if}}>{{label}}</option>
        {{/each}}
      </select>
    </div>
    <div class="lightning-graphic"></div>
    <div class="field-group notes-group">
      <label class="field-label">Notes:</label>
      <input type="text" name="system.sessionNotes" value="{{system.sessionNotes}}" placeholder="Session notes..." />
    </div>
  </div>
</div>
